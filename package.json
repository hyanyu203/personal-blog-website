{
  "name": "jiangou",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14.21.3 <15",
    "npm": ">=6 <7"
  },
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev:backend": "cd backend/monolith && mvnw.cmd spring-boot:run",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "install:all": "npm install --prefix packages/shared && npm install --prefix frontend && npm install --prefix admin"
  }
}
